public static Integer findLargest(Integer num1, Integer num2, Integer num3) {

    // Inserindo chamada encadeada da função Math.max()
    Integer result = Math.max(num1 Math.max(num2, num3));

    return result;

}